/**
 * Application-owned synthetic media map. Keep this list explicit: graph
 * assertions identify evidence, but never provide browser paths or URLs.
 */
export const SYNTHETIC_MEMBER_ASSET_ALLOWLIST = Object.freeze({
  "assertion:8d2067e51d8eb4e7": "/synthetic/jordan-home-equipment.svg",
} as const);

