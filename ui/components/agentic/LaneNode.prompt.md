One node in a decision-lane stack (MEMBER → ANATOMY → RULE → WORKOUT). Signal-faint wash + mono source; join nodes with a centered ↓.

```jsx
<LaneNode text="patellofemoral joint ⊂ knee joint" source="SNOMED CT SUBSET" />
<LaneNode empty />
```
