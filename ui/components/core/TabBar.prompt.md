Bottom tab bar; active tab gets a short ink bar above the label. Navigation is a human act, so the indicator is ink — Signal never marks navigation.

```jsx
<TabBar tabs={['Today','Copilot','History']} active={tab} onChange={setTab} />
```
