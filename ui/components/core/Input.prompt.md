Text input / textarea on recessed bg with hairline. Required-reason fields (overrides) use multiline.

```jsx
<Input multiline placeholder="Reason (required) — e.g. cleared by PT" value={v} onChange={...} />
```
