Pill-shaped action button; primary = carbon ink (human decisions like Approve), never Signal-colored.

```jsx
<Button variant="primary" size="lg" onClick={approve}>Approve & publish</Button>
<Button variant="secondary">Adjust</Button>
```

Variants: primary/secondary/outline/ghost; sizes md (44px) / lg (52px); disabled state uses --ax-disabled-*.
