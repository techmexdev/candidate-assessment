Dot-and-line version history; every entry names actor, time, and changes. Signal dot marks overrides.

```jsx
<VersionTimeline versions={[{title:'v3 · Safety override', meta:'COACH SAM · 7:48 AM', signal:true, changes:['Warning retained']}]} />
```
